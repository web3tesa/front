export enum EDexPlatform {
  DEXSCREENER = "dexscreener"
}

export enum RequestMethod {
  GET = "get",
  POST = "post",
  PUT = "put",
  DELETE = "delete"
}

export enum ETradeStatus {
  INIT,
  PENDING,
  SUCCESS,
  FAILED,
}

export enum ELanguage {
  EN = "en",
  ZH = "zh"
}